using System.IO;

namespace Minecraft
{
    class InteractionFactory
    {
        /*public BlockInteraction ReadInteraction(Connection connection)
        {
            BinaryReader reader = connection.reader;

            byte interactionType = reader.ReadByte();
            InteractionType type = (InteractionType)interactionType;

            switch (type)
            {
                case InteractionType.None:
                    {

                    }
            }
        }*/
    }
}
